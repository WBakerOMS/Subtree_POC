#include <iostream>
#include <vector>
#include <string>

#include "core/core.hpp"

using namespace std;

int main()
{
    for (int i = 0; i < 10; i++)
    {
        id_check(i);
    }

    return 0;
}
